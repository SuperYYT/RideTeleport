name: RideTeleport
main: ridetp\RideTeleport
version: 1.0
api:
 - 3.0.0
 - 3.2.3
load: POSTWORLD
author: vardo

permissions:
  rtp.*:
    default: op
    description: RideTeleport的设定命令
    children:
      rtp.command:
        default: op
        description: RideTeleport的设定指令
